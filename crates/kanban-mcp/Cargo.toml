[package]
name = "agentkanban-mcp"
version = "0.2.0"
edition = "2021"
license = "MIT"

[dependencies]
kanban-core = { path = "../kanban-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
rusqlite = { version = "0.40.2", features = ["bundled"] }
tempfile = "3"
