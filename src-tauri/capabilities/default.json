{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local board capture, feedback and user review. External HTTP(S) links use a validated Rust command; local files are never opened.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:deny-open-path",
    "opener:deny-open-url",
    "opener:deny-reveal-item-in-dir"
  ]
}
